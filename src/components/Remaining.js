import react from 'react';

const Remaining = ( ) => {
    return(
        <div className='alert alert-success'>
            <span>Remaining: $12000</span>
        </div>
    );
};

export default Remaining;